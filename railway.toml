[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[[services]]
name = "api"
source = "."
dockerfilePath = "Dockerfile"
startCommand = "node server.js"
healthcheckPath = "/health"

  [services.variables]
  PORT = "3000"
  NODE_ENV = "production"
